const Timer = () => {
    return (
        <div className="timer-container">
            <h2 id="timer-label">Session</h2>
            <p id="time-left">25:00</p>
        </div>
    );
};

export default Timer;
